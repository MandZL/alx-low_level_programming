#include "main.h"
/**
 * swap_int - swaps the value of two intergers
 * int n - int to swap
 * int a, int b vlues to be swapped
 */

void swap_int(int *a, int *b)
{
	int n;
	n = *a;
	a* = b*;
	*b = m;
}
