#include <stdio.h>
/**
 * main - writes all single digit 
 * numbers of base 10starting from 0
 * Return: always 0 (success)
 */
int main(void)
{
	int n;
	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	print("\n")
	return (0);
}
