#include <stdio.h>
/**
 * main - prints all the numbers of base 16 in lowercase, followed by a new line.
 * use ptchar only
 * Return: always 0 (sucess)
 */
int main(void)
{
	int n;
	char x;
	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}
	for (x = 'a'; x <= 'f'; x++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}

