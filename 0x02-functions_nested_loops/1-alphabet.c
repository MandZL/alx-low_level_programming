#include "main.h"
/**
 * main -print alphabet in lower casex10
 *
 * Return 0 always success
 */
void print_alphabet(void)
{
	int n;
	for (n = 'a', n <= 'z', n++)
	{
		_putchar(n)
	}
	putchar('\n')
}
